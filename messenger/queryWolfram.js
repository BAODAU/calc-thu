'use strict'
var APIKEY = proces.env.WOLFRAMKEY;

const rp = require('minima-request-promise')
const wolfram = require('wolfram').createClient(WOLFRAMKEY)

function queryWolfram(request, originalApiRequest) {
    
}
